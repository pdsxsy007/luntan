<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="convert.myapp.com.myapplication">

    <application
        android:allowBackup="true"
        android:name=".application.MyApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".avtivity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".avtivity.ArticleDetailsActivity" />
        <activity android:name=".avtivity.MainActivity" />
        <activity android:name=".avtivity.RegiestActivity" />
        <activity android:name=".avtivity.SendArticleActivity" />
        <activity android:name=".avtivity.LoginActivity" />
        <activity android:name=".avtivity.ChooseActivity" />
        <activity android:name=".avtivity.ManagerActivity" />
        <activity android:name=".avtivity.ColletActivity" />
        <activity android:name=".avtivity.MyDeleteActivity" />
    </application>

</manifest>